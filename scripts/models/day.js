(function() {    
    FoodPlanner.classes.models.Day = Backbone.Model.extend({

        defaults: {
            id:     0,
            name:   'day',
            dish:   'fill in dish'
        }
    });

})();
